<html>
<body>
	<h1>Find Your Next Bus!</h1>
	<p>Please select your route below</p>
	<p>Serving LA area, specifically LA Metro</p>
	<h2>Route: {{routeSelected}}</h2>
	<h2>Directions</h2>
	<form method="get" action="/stops">
		<select name="direction_selected">
		{{#each directions}}
			<option value='{{this.json}}'>{{this.name}}</option>
		{{/each}}
		</select>
		<button type="Submit" name="submitDirection" value="submitDirection">Submit Direction</button> 
	</form>
</body>
</html>
